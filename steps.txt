install vite,node
open cmd :
>npm create vite@latest
project name:ai-resume-analyzer
select framework:react
select a variant:javascript

>cd ai-resume-analyzer
>npm install
>npm run dev

public folder contains images which can be accessible publicly without login
assets folder contains images after login hidden from public